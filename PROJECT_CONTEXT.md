# PROJECT_CONTEXT

**이 문서 하나만 읽고도 이어서 작업할 수 있게** 정리한 것입니다.
마지막 갱신 2026-08-07. 기준 커밋 `94e6e82` (배포까지 끝난 상태).

> **2026-08-07 에 한 일** — 예상 금액에서 「추가금」이라는 말을 전부 걷어냈고,
> 디자인 이름에서 「무지」를 뗐고, 양면 베개커버를 1장당 값으로 바꿨고,
> 「베개커버도 앞뒤를 다르게」 체크칸을 없앴고, 매트리스 실제 치수를 필수로 만들었고,
> **복사한 주문을 카카오톡 채널로 넘기는 길을 붙였습니다.**

> 사람이 읽을 문서는 `README.md` 입니다. 이 문서는 **다음 작업자를 위한 인수인계서**입니다.
> 둘이 어긋나면 **코드가 정답**입니다 — 코드에 왜 그렇게 했는지가 주석으로 다 적혀 있습니다.

---

## 1. 이게 뭐하는 것인가

**파마파미**(FAMAFAMI) 침구 브랜드의 **맞춤 주문 창구**입니다.
손님이 이불·매트리스커버·베개커버의 색을 **원단 100색**에서 골라 미리 보고,
사이즈까지 정한 뒤 **주문 내용을 파마파미 카카오톡 채널로 보내는** 한 장짜리 웹페이지입니다.

- 배포 주소 **https://famafami83-del.github.io/famafami-color-simulator/**
- 저장소 `famafami83-del/famafami-color-simulator` (public)
- 결과물은 **`index.html` 한 장** — 사진·마스크·폰트가 전부 그 안에 들어 있습니다 (base64).

### 손님이 지나가는 길

| 단계 | 하는 일 |
|---|---|
| **① 디자인** | **브랜드 표지.** 사진 + 워드마크 + 슬로건, 그 아래 상품 카드 2장 |
| **② 색** | 부위를 눌러 원단 100색에서 색을 고른다. 사진에 즉시 반영 |
| **③ 사이즈** | 이불·매트리스커버·베개커버의 종류·사이즈·수량. 안 살 것은 「제외」 |
| **④ 확인** | 예상 금액 + 주문서. **「카톡으로 주문하기」 → 복사되고 카톡 채널이 열림** |

---

## 2. 색이 입혀지는 원리 — 이걸 모르면 아무것도 못 고칩니다

**침구가 전부 흰색인 사진 한 장** 위에, 부위마다 **알파 마스크 PNG**를 씌운
`<div>`를 얹고 `mix-blend-mode: multiply` 로 색을 곱합니다.

```
흰 바탕 사진  ×  고른 색  =  그 색으로 물든 침구 (주름·그늘은 그대로 살아 있음)
```

**반드시 기억할 것**

- **검정 × 무슨 색 = 검정.** 사진에서 검게 나온 자리는 **영원히 색이 안 먹습니다.**
  날개형 사진이 막혀 있는 이유가 이것입니다 (날개가 검정).
- 마스크는 **투명도(알파)** 를 읽습니다. 흑백 그림으로 두면 브라우저가 무시해서
  **색을 눌러도 아무 일도 안 일어납니다.**
- 마스크는 **색이 변하는 자리를 비교해서** 뽑습니다. 그래서 같은 구도로
  **흰 바탕 1장 + 색이 서로 다른 렌더 2~3장**이 필요합니다.

### 디자인마다 사진과 마스크가 따로입니다

| 디자인 | 바탕 사진 | 마스크 | 고르는 부위 |
|---|---|---|---|
| **같은 컬러** (`plain`) | `base.jpg` 1200×1500 | `mask_{quilt,mattress,pillowF,pillowR,pillowL,pillowW}.png` | 6곳 |
| **다른 컬러** (`both`) | `base_both.jpg` 1200×1600 | `mask_{bothA,bothB,bothM,bothP}.png` | 3곳 |

- `both` 는 **이불과 베개를 따로 못 고릅니다.** 색 사진 3장이 전부 베개를 이불과
  같은 색으로 칠해놔서 둘을 가를 수가 없습니다. 대표가 원한 것이 그것이라 그대로 둡니다.
  그래서 부위 이름이 `이불·베개 앞면` 입니다.
- `bothP` 는 **삥(테두리)** 입니다. 잡아뒀지만 `follow:'bothA'` 로 앞면 색을 따라가
  봉제선처럼 보입니다 — 이 제품에는 삥이 없기 때문입니다.

---

## 3. 파일 구조

```
index.html                  ← 배포되는 것. 직접 고치지 말 것 (build.js 가 덮어씀)
colors.json                 ← 팔레트 100색 공개용. build.js 가 만듦
.nojekyll                   ← 깃허브가 쓸데없이 Jekyll 돌리지 않게
.github/workflows/deploy.yml ← 우리가 직접 만든 배포 (아래 8장 참조)
README.md                   ← 사람이 읽는 문서 (대표도 읽는다)
PROJECT_CONTEXT.md          ← 이 문서

src/
  build.js                  ★ 거의 모든 것이 여기 있다. 1,460줄. 대부분 여기만 고친다
  swatches.json             원단 102색 원본. 컬러차트를 그대로 옮긴 것 — 손대지 말 것
  assets/                   완성된 사진·마스크·폰트 (도구가 만든 것)
  tools/                    원본 소재가 바뀌었을 때만 쓰는 재생성 도구
    README.md               도구별 사용법과 함정 — 도구 고치기 전에 읽을 것
    extract-masks.js        무지 구도 마스크 6장 + base.jpg
    alpha-masks.js          위 마스크를 알파 채널로 (건너뛰면 색이 안 먹음)
    split-piping.js         양면 구도 base_both.jpg + 마스크 4장
    design-cards.js         디자인 카드 사진 2장
    brand-hero.js           ① 표지에 까는 사진
    brand-logo.js           FAMA FAMI 워드마크 (투명도만 담긴 그림)
    subset-fonts.js         ★ 폰트를 쓰는 글자만 잘라내기 (아래 6장)
    extract-colors.js       컬러차트에서 102색 뽑기
    verify.js               브라우저 없이 마스크 확인
    preview.js              ★ 올리기 전에 폰으로 보는 서버 (아래 7장)
    deploy-watch.sh         배포 감시 + 막히면 자동으로 뚫기
    deploy-wait.sh          깃허브 장애가 끝나기를 기다렸다 배포
```

### `src/build.js` 안의 지도

줄 번호는 **`94e6e82` 기준**입니다. 고치면 밀리니 이름으로 찾는 편이 빠릅니다.

| 줄 근처 | 무엇 |
|---|---|
| 24 | `EXCLUDE` — 팔레트에서 뺄 색 번호 |
| 47 | `NO_FIX` — 컬러차트에 잘못 찍힌 번호 정정표 |
| 78 | `WHITE` — 모든 부위의 시작 색 (NO.952) |
| 104 | `NOPAT` / `DESIGNS` — 디자인 정의 (`on` = 밖으로 나가는 이름) |
| 116 | `CARRY` — 디자인을 바꿀 때 색을 물려주는 규칙 |
| 124 | `PARTS` — 색을 칠하는 자리들 |
| 193 | `PIL_MODES` — 베개커버 칸 구성 (single / bothTwo / ~~bothPlain~~) |
| 234~263 | 사이즈·수량 목록 (`MAT` `OZ` `QUILT_KIND` `MAT_KIND` `PILLOW` `PIL_QTY`) |
| 288 | **`PRICE`** — 가격표. 값 고칠 일은 여기뿐 |
| 383 | `BRAND` — ① 표지 문구 |
| **407** | **`INQUIRY`** — 복사한 주문을 들고 갈 **카톡 채널 채팅방 주소** |
| 419 | `ALWAYS_CUSTOM` |
| 481~494 | `FACE` / `FONT_BODY` / `FONT_HEAD` — 폰트 |
| 1052 | `NEXT_LABEL` — 단계마다 아래 단추 이름. 마지막은 `INQUIRY` 유무를 따라감 |
| 1191 | `SUB` — 단계마다 머리글 아래 한 마디 |
| 1334 | `MAT_DIMS` — ④ 로 넘어가려면 적어야 하는 매트리스 칸 |
| 1341 | `checkHeight()` — 매트리스 칸 경고와 빨간 표시 |
| 1429~ | `quote()` `renderQuote()` `renderOrder()` — 견적과 주문서 |

---

## 4. 지금 구현된 것

### ① 디자인 = 브랜드 표지

- 폰에서는 **표지만 한 화면**에 담습니다 (`100svh - 76px`). 카드는 아래로 내려갑니다.
- 그래서 **「디자인 고르기 ↓」** 를 표지 맨 아래에 붙였습니다. 없으면 카드를 못 보고
  아래 단추를 눌러 기본값으로 넘어갑니다.
- 워드마크는 **일부러 잘립니다** — 가로 104%, 아래로 22% 내려 사진 밖을 잘라냅니다.
  통째로 앉히면 그냥 머리글이 되고, 잘라야 표지로 읽힙니다.
- 사진 아래쪽만 어둡게 깝니다 (`.bshot::after`). 크림색 워드마크가 밝은 침구 위에서
  사라지기 때문입니다. **사진을 바꾸면 이 값(0.56)도 같이 봐야 합니다.**
- **처음엔 어느 카드에도 고른 표시를 하지 않습니다** (`chosen` 깃발).
  카드를 안 누르고 아래 단추로 넘어가면 그때 기본 디자인으로 정해진 것으로 봅니다.

### ② 색

- 부위 알약을 눌러 고릅니다. 폰에서는 **두 칸씩** 줄을 맞춥니다 (700px 이하).
- **매트리스커버는 60·80수만** 고를 수 있습니다 (100수는 얇아서 안 씀) — `PARTS.su`.
- **`다른 컬러` 를 고르면 베개커버도 무조건 양면입니다** [대표, 2026-08-07].
  묻던 체크칸(「베개커버도 앞뒤를 다르게」)은 없앴습니다 — 사진이 이미 베개 앞뒤를
  다르게 보여주고 있어 체크를 푸는 쪽이 사진과 어긋났습니다.
  `PIL_MODES.bothPlain` 과 `.ptwo` 스타일은 되살릴 때 쓰려고 남겨뒀습니다.
- 디자인을 바꿔도 **아직 손 안 댄 부위**에는 쓰던 색을 물려줍니다 (`CARRY`, `touched`).

### ③ 사이즈

세 칸 모두 같은 짜임입니다 — **제목 → 경험형 안내 → 보조 안내 → 고르는 칸 → 제외 체크**

- 이불은 **종류를 먼저** 고릅니다. 종류마다 사이즈 목록과 가격표가 다릅니다
  (차렵이불 슈퍼싱글 150×210 / 이불커버 160×210).
- 이불커버는 두께(온스) 칸이 사라지고 **똑딱이 갯수** 칸이 나옵니다.
- 매트리스는 **가로·세로·높이를 각각** 받습니다. 한 칸에 「150x200」으로 받았더니
  폰 숫자 자판에 `x` 가 없어 못 적었습니다.
- 높이별 추가 금액은 **접어둡니다** (`<details>` 「높이 안내」).
- 베개커버 칸 수는 디자인에 따라 다릅니다 — 무지 4칸, 양면 1칸.

**★ 매트리스 실제 치수를 다 적어야 ④ 로 넘어갑니다** [대표, 2026-08-07] (`MAT_DIMS`).
만들 때 쓰는 치수라 없으면 주문이 성립하지 않고, **높이는 값까지 달라집니다.**
전에는 예상 금액에서 「적어주세요」로 알리기만 해서 그냥 지나칠 수 있었습니다.

- **빈 칸을 빨갛게 짚는 것은 한 번 막힌 뒤부터입니다** (`matTried`). 들어오자마자
  빨간 칸을 보여주면 적을 기회도 없이 혼난 것처럼 보입니다.
- 막을 때는 **첫 빈 칸으로 화면을 옮기고 커서까지 넣습니다.** 못 넘어간다고만 알리면
  폰에서는 어느 칸인지 몰라 헤맵니다.
- 「숫자가 아닌 값」도 안 적은 것으로 봅니다 (`dim()` 이 `null`). 「백오십」은 치수가 아닙니다.
- **「매트리스커버는 안 할래요」면 묻지 않습니다.** 안 사는 것을 물을 이유가 없습니다.
- 높이 **65cm 초과는 그대로 넘어갑니다.** 값이 제대로 적힌 것이라 막을 이유가 없고,
  ④ 에서 빨간 「주문 불가」로 빠집니다.

### ④ 확인

- 예상 금액 + 복사용 주문서. `PRICE` 가 전부 비면 금액 화면이 아예 안 나옵니다.
- 높이가 65cm 를 넘으면 **「주문 불가」** 로 빠집니다 (문의로 넘기지 않음).

**★ 예상 금액에서는 「추가금」이라는 말을 쓰지 않습니다** [대표, 2026-08-07].
맞춤 추가금·높이 추가금·양면 베개커버 값은 **전부 단가에 녹여** 계산하고,
줄에도 안내에도 드러내지 않습니다. 이미 들어간 값인데 적어두면 **또 붙는 것으로 읽힙니다.**
그래서 이런 것들이 없습니다 — 「맞춤 제작 추가금이 포함된 금액입니다」,
「높이 40cm (+15,000원)」, 「베개커버 양면 추가 : 10,000원」, 체크칸의 「2장까지 10,000원」.

- 이불 줄은 **고른 결과만** 적습니다 — `앞뒤 같은 컬러 · 차렵이불 · 8온스 · 퀸 200×230`.
  두께는 차렵이불에만 나오고 괄호 안만 씁니다 — 다 적으면 폰에서 줄이 접힙니다.
- 주문서 아래 안내는 **대표가 준 문구** 그대로입니다. 복사되는 주문서 안에는
  아무 말도 붙이지 않습니다 — 「복사해 문의해주시면」이 이미 복사된 글에 들어가면 순환이 됩니다.
- 합계 아래에는 **고정 안내 두 줄**만 둡니다 (`.qfoot`). 그 주문에만 뜨는 말
  (주문 불가, 똑딱이 갯수, 양면인데 같은 색)은 `#qNote` 에 따로 나가고 없으면 감춥니다.

### ④ 마지막 단추 — 카톡으로 주문하기

**복사한 주문을 파마파미 카카오톡 채널로 넘깁니다** [대표, 2026-08-07].

```
누름 → ① 주문 복사 → ② 알림 띄움 → (0.7초) → ③ 카톡 채팅방 열림
                                            → 손님이 붙여넣어 보냄
```

- 주소는 **`INQUIRY` 한 줄**입니다. 비우면 단추 이름까지 「복사하기」로 돌아갑니다 —
  갈 곳이 없는데 「카톡으로 주문하기」라 달면 눌러도 안 열려 거짓말이 됩니다.
- **`/chat` 을 빠뜨리면 빌드가 멈춥니다.** 채널 홈은 채팅방이 아니라 소개 화면입니다.
  카카오 관리자센터는 `http://` 로 알려주지만 **`https://` 로 적습니다.**
- **알림을 먼저 띄우고 0.7초 뒤에 넘어갑니다.** 바로 넘어가면 「복사되었습니다」가
  보일 새가 없습니다. 알림은 **지우지 않습니다** — 카톡에서 돌아오시면 그대로 남아
  무엇을 하던 중이었는지 알 수 있습니다. ③ 으로 되돌아가면 그때 지웁니다.
- 알림에 **「카카오톡이 안 열렸다면 여기를 눌러주세요」** 를 넣었습니다. PC 처럼 자동
  이동이 막히는 자리에서 빠져나갈 길이 없으면 거기서 끝납니다.
- `window.open` 이 아니라 `location.href` 를 씁니다. 클립보드를 기다린(`await`) 뒤에
  여는 창은 폰 브라우저가 「손님이 누른 것이 아니다」로 보고 막는 일이 있습니다.

**★ 「그대로 전송」은 못 합니다.** 카카오는 **링크에 메시지 내용을 담아 보내는 길을
열어두지 않았습니다.** 알림톡·친구톡은 사업자→고객 방향이고(대행사 계약·발신프로필
심사 필요), 카카오톡 메시지 API는 손님이 카카오 로그인을 해야 하며 **채널로는 못
보냅니다.** 그래서 **손님이 붙여넣기 한 번**을 해야 합니다. 자동 접수를 하려면 서버가
있어야 하고, 그때도 카톡이 아니라 폼·시트로 받게 됩니다.

**★ 손님이 보낸 말은 대표 개인 카톡으로 오지 않습니다.** 「카카오톡 채널 관리자」 앱이나
관리자센터(`business.kakao.com`)로만 들어갑니다. 개인 계정과 채널은 분리돼 있습니다.
대표가 자기 채널에 손님으로 들어가면 개인 카톡에 방이 생기는데, 그것은 **손님 쪽 화면**입니다.

---

## 5. 디자인 · 카피 원칙 (대표가 정한 것)

### 색

| | 밝은 화면 | 어두운 화면 |
|---|---|---|
| 바탕 `--bg` | `#f3f0e9` 크림 | `#131a2a` 짙은 네이비 |
| 글자·강조 `--fg` | **`#16203d` 브랜드 네이비** | `#ece9e1` 크림 |
| 흐린 글씨 `--mut` | `#636a7b` | `#98a0b2` |
| 카드 `--card` | `#fdfbf6` | `#1a2338` |

- **`--fg` 하나만 바꾸면 강조가 전부 따라옵니다** — 고른 부위 알약, 「다음」 단추,
  단계 표시, 색칩 선택 테두리가 모두 `var(--fg)` 를 배경으로 씁니다.
- **`--card` 는 크림보다 밝게 둘 것.** 카드까지 크림이면 팔레트의 흰색 계열 색칩이 묻힙니다.
- **시그니처 버터 `#f4ecb8`** (직조라벨 SH-295 버터베이지) 는 **지금 안 씁니다.**
  주 단추에 칠해봤더니 네이비가 더 세련돼 보인다는 판단이었습니다.
  큰 면적에 깔면 무르게 보이는 색이라, 다시 쓰면 **좁은 자리**에 쓰십시오.

### 글자 크기 — 숫자를 박지 말고 계단에서 고를 것

```css
:root{--t1:16px;--t2:13.5px;--t3:12.5px;--t4:11.5px;--t5:10.5px}
```
구역 제목 / 칸 제목 / 본문 / 곁말 / 아주 작은 라벨. 한 곳만 고쳐도 크기 관계가 유지됩니다.

### 글 쓰는 법

- **한 문장에 한 줄.** 줄바꿈은 **뜻이 끊기는 자리**에 `<br>` 로 손수 넣습니다.
  넓은 화면이라고 한 줄로 늘이지 않습니다 — 폰과 같은 호흡이 낫습니다.
- `word-break:keep-all` 을 걸어 어쩔 수 없이 넘어갈 때도 **낱말 가운데가 아니라
  띄어쓰기에서** 끊기게 합니다.
- **화면 구조를 설명하지 않습니다.** 「②에서 고르신 4칸이…」 같은 말은 손님이 알 바가
  아닙니다. 고른 것에 따라 **달라지는 말만** 동적으로 넣습니다.
- **줄임표(…)를 쓰지 않습니다.** 눌러야 할 단추 이름이 잘리면 무엇인지 알 수 없습니다.
- 「무지/양면」과 「같은/다른 컬러」처럼 **두 말을 섞지 않습니다.** 한 페이지 안에서
  같은 것은 같은 말로 부릅니다.
- 여백을 글자보다 중요하게 봅니다. 빽빽한 주문서가 아니라 하나씩 골라가는 자리로 보이게.

### 이름 짓기

- 화면에는 **짧은 이름**(`DESIGNS.ko` — 같은 컬러 / 다른 컬러).
- 예상 금액·주문서에는 **긴 이름**(`DESIGNS.on` — 앞뒤 같은 컬러 / 앞뒤 다른 컬러).
- **「무지」는 어디에도 쓰지 않습니다** [대표, 2026-08-07]. 2026-08-06 까지는 대표가 불러온
  이름이라 주문서에 「무지 · 앞뒤 같은 컬러」로 같이 적었는데, **패턴이 없다는 점은 두
  디자인이 똑같아서** 가리는 말이 못 되고 같은 말만 겹쳐 보였습니다.
  베개커버 주문서도 같은 말을 쓰므로 이제 온 페이지가 한 이름으로 부릅니다.

---

## 6. 폰트 — ★ 제일 놓치기 쉬운 함정

**제목 Paperlogy Bold 700 / 본문 Pretendard Regular 400** 을 페이지에 심었습니다.

한글 폰트는 통째로 두 벌 3MB 라 못 넣습니다. **페이지가 실제로 쓰는 글자만**
잘라 넣습니다 (2026-08-07 기준 **653자**, 한글 542자 → 두 벌 100KB).

### 문구를 바꾸면 반드시 다시 자를 것

```bash
node src/build.js               # 글자가 다 들어간 index.html
node src/tools/subset-fonts.js  # 그 글자만 남긴 woff2 두 벌
node src/build.js               # 이번엔 폰트가 박힌다
```

**두 번 돌리는 이유** — 글자 목록을 **만들어진 `index.html`** 에서 뽑기 때문입니다
(문구를 코드 여기저기서 만들어 소스만 봐서는 셀 수가 없습니다).
폰트가 없으면 build 는 멈추지 않고 **경고만** 합니다 — 처음 만들 때는 폰트가 있을 수 없습니다.

**안 자르면 새로 들어간 글자만** 기기에 깔린 글꼴로 떨어집니다. 한 글자씩이라
**눈에 잘 안 띕니다.** 실제로 「?」「떤」「까」「↓」를 그렇게 놓칠 뻔했습니다.

**심은 글꼴 이름을 `FF Head`/`FF Body` 로 원본과 다르게 둔 것은 일부러입니다.**
잘라낸 글꼴에 없는 글자(손님이 「요청사항」에 적는 글자)가 나오면 뒤에 적어둔
기기 폰트로 자연스럽게 떨어집니다. 이름을 `Pretendard` 로 똑같이 주면 그 길이 막힙니다.

원본 폰트는 `%LOCALAPPDATA%\Microsoft\Windows\Fonts\` 에 있습니다
(`Paperlogy-7Bold.ttf`, `Pretendard-Regular.otf`).
윈도우가 「모든 사용자」로 설치하면 `C:\Windows\Fonts` 로 가므로 도구가 두 자리를 다 봅니다.

---

## 7. 작업 흐름 — 올리기 전에 폰으로 본다

대표는 **늘 폰으로 확인합니다.** 그래서 순서가 이렇습니다:

```bash
node src/tools/preview.js        # 미리보기 서버 (같은 와이파이의 폰에서 열림)
# 고친다 → node src/build.js → 폰에서 새로고침 → 대표 확인
# 「올려」 하시면 그때 커밋 + 푸시
```

- 미리보기는 **캐시를 꺼놨습니다.** 고칠 때마다 새로고침만 하면 최신입니다.
- **안 열리면 컴퓨터 IP가 바뀐 것**을 먼저 의심하십시오 (와이파이/핫스팟 전환).
  실제로 `192.168.123.103` → `172.20.10.2` 로 바뀐 적이 있습니다.
- 서버에 시간 제한이 걸려 꺼질 수 있습니다. 포트가 잡혀 있으면 `node` 프로세스를 끄고 다시 띄웁니다.

---

## 8. 배포 — 여기서 제일 많이 데었습니다

`.github/workflows/deploy.yml` 로 **우리가 직접** 돌립니다.
자리 이름(concurrency group)이 `famafami-pages` 입니다.

**깃허브가 붙여주던 `pages-build-deployment` 는 쓰지 않습니다.**
2026-08-06 에 그 배포 하나가 `queued` 인 채로 굳어버렸고 취소도 안 됐습니다
(`Cannot cancel a workflow re-run that has not yet queued`). 그놈이 `pages` 자리를
붙잡아 **뒤에 올린 배포가 전부 줄만 섰습니다.** 그래서 다른 줄로 옮겼습니다.

### 규칙

- **커밋을 연달아 밀지 말 것.** 앞 배포가 끝나기 전에 밀면 서로 취소시킵니다.
  6분 간격으로 두 번 밀었다가 **4시간 막혔습니다.** 모아서 한 번만 밉니다.
- **「몇 분 뒤 반영됩니다」로 끝내지 말 것.** 라이브를 열어 **찾는 말이 있는지** 확인하고
  보고합니다. 「배포 성공」이라 해놓고 옛 파일을 내주는 것도 겪었습니다.
- **깃허브 전체 장애일 수 있습니다.** 2026-08-06 에 Actions·Pages 가 9시간 대규모
  장애였습니다. 저장소를 뒤지기 전에 `githubstatus.com` 을 먼저 보면 헛수고를 안 합니다.

막혔을 때 뚫는 방법은 `src/tools/deploy-watch.sh` 가 자동으로 합니다 —
① 앞 배포가 막으면 취소 ② 시간초과면 다시 돌림 ③ **queued 로 굳으면 새 커밋으로 부름.**
③ 이 고약합니다. queued 는 실패가 아니라 대기라, 실패만 보고 있으면 영영 안 깨어납니다.

---

## 9. ★ 건드리면 안 되는 것

| 대상 | 왜 |
|---|---|
| **`index.html` 직접 수정** | `build.js` 를 돌리면 통째로 덮어씁니다. 반드시 `src/build.js` 를 고칠 것 |
| **`src/swatches.json`** | 컬러차트를 그대로 옮긴 **원본**입니다. 틀린 번호는 `build.js` 의 `NO_FIX` 에서 바로잡고, 팔지 않는 색은 `EXCLUDE` 로 뺍니다. 원본을 고치면 차트를 다시 뽑을 때 어긋납니다 |
| **`PRICE.pillow.custom` 을 `null` 로** | 반드시 **`0`** 이어야 합니다. `null` 이면 「가격 문의」로 새어나갑니다 |
| **가격 계산 로직** | 대표가 값만 정하고 구현은 맡깁니다. 값은 `PRICE` 숫자만 고치면 됩니다 |
| **팔레트 키 `c0`, `c1`…** | 팔레트 **순번**이라 색이 늘거나 빠지면 밀립니다. 화면 안에서만 쓰고 **주문 기록처럼 오래 남는 곳에 저장하면 안 됩니다.** 저장에는 번호+이름을 씁니다 |
| **`--fg` 를 강조 아닌 값으로** | 강조 자리 전부가 이 값을 배경으로 씁니다 |
| **저장소 주소** | 바꾸면 지금까지 공유한 링크가 다 끊깁니다. 화면 이름만 `FAMAFAMI MADE` 로 바꿨습니다 |
| **`.nojekyll` 삭제** | 쓸데없는 Jekyll 단계가 되살아납니다 |
| **로고 시트 `02. 브랜드 로고/로고.png` 사용** | 아래 영문이 `INSTITUE`(오타)입니다. `자사몰 로고.png` 를 쓰십시오 |
| **`<meta name="referrer">` 삭제** | 지우면 **카톡에 붙는 「이전 페이지」 링크가 다시 404** 가 됩니다 (바로 아래) |

**빌드가 멈추는 자리가 22곳 있습니다** (`throw new Error`). 전부 **조용히 잘못된 페이지가
나가는 것을 막으려고** 넣은 것입니다 — 마스크 없음, 카드 사진 없음, 가격표와 사이즈 목록
불일치, 번호 중복, 시작 색이 팔레트에 없음, `INQUIRY` 형식 틀림 등.
**에러가 나면 지우지 말고 원인을 고치십시오.**

### 카톡에 붙는 「이전 페이지」가 404 였던 일 [2026-08-07]

카톡 채팅방이 열릴 때 **카카오가 유입 주소를 자동으로 적어 보냅니다.** 그런데
`famafami83-del.github.io` 까지만 찍혔습니다 — 이 페이지는 그 아래
`/famafami-color-simulator/` 에 있고 **루트에는 아무것도 없어 404** 가 떴습니다.
손님도 대표도 그 화면을 봤습니다.

브라우저는 **다른 도메인으로 넘어갈 때 주소의 앞부분만 넘기는 것이 기본**입니다
(`strict-origin-when-cross-origin`). 그래서 `<head>` 에 이 한 줄을 두었습니다:

```html
<meta name="referrer" content="unsafe-url">
```

이름이 무섭지만 **이 페이지는 주소에 개인정보가 실리지 않습니다** — 고른 색도 사이즈도
주소가 아니라 화면 안에만 있습니다. **저장소를 옮기거나 주소 구조를 바꾸면 이 자리를
다시 확인하십시오.**

---

## 10. 가격 (2026-08-07 기준)

- 맞춤 추가금 — 이불·매트리스커버 **+10,000원**(1장당). **베개커버는 없음.**
- 이 페이지 주문은 전부 맞춤이라 `ALWAYS_CUSTOM = true`.

| | |
|---|---|
| 차렵이불 | 슈퍼싱글 195,000 / 퀸 265,000 / 킹 290,000 / 라지킹 340,000 |
| 이불커버 | 슈퍼싱글 170,000 / 퀸 190,000 / 킹 210,000 / 라지킹 240,000 |
| 올인원 매트리스커버 | 140,000 ~ 260,000 (규격 10가지) |
| 홑겹 매트리스커버 | 110,000 ~ 195,000 |
| 베개커버 | **28,000** (1장당, 사이즈 무관) |
| **양면 베개커버** | **33,000** (1장당) — 28,000 + 5,000 (`PRICE.pillow.two`) |
| 매트리스 높이 | 35cm까지 없음 / ~45 +15,000 / ~55 +25,000 / ~65 +35,000. **65cm 초과 주문 불가** |

> 2026-08-04 에 베개커버를 「26,000 + 맞춤 3,000」으로 받아 적었는데 **잘못이었습니다.**
> 대표가 「두 장이면 56,000원 아닌가」를 짚어주면서 드러났습니다. **값은 반드시 확인받을 것.**

---

## 11. 아직 해야 할 일

### 곧 할 수 있는 것

- **삥(테두리) 디자인 추가** — 마스크(`mask_bothP.png`)와 사진이 **이미 페이지 안에**
  있습니다. `PARTS` 의 `bothP` 에서 `follow` 를 지우고 `DESIGNS` 에 카드 한 줄만 더하면
  됩니다. 카드 사진만 새로 만들면 되고 **+33KB** 뿐입니다.
  대표 말: **「디자인을 다 수정하고 넣는 게 낫겠다」** — 문구·디자인 정리가 끝난 뒤에.
  값(무지와 같게 할지, 얼마를 더 받을지)을 물어야 합니다.

### 사진이 있어야 되는 것

- **날개형 디자인** — `.tif` 원본(3.2GB)에서 **① 날개도 매트리스도 흰색인 바탕 +
  ②③ 자리마다 색이 다른 렌더 2장**을 내보내야 합니다.
  받은 「바탕」이 `날개(검정).jpg` 와 바이트까지 같은 복사본이었고, 날개가 검정이라
  색을 못 입힙니다.
- **양면에서 베개를 따로 고르기** — 지금 세 장이 전부 베개를 이불과 같은 색으로
  칠해놨습니다. **베개만 이불과 다른 색으로 칠한 렌더 한 장**이면 됩니다. 급하지 않습니다.

원본은 `D:\01. 파마파미\00. 디자인\컬러시뮬레이션\` 의 `.tif`/`.psb` 입니다.

### 용량 — 디자인이 셋이 되면 반드시 할 일

지금 `index.html` **1,413KB 중 84%(1,188KB)가 사진**입니다.

| | 늘어남 | 결과 |
|---|---|---|
| 삥 추가 | +33KB | 1,446KB |
| 날개형 추가 | +380KB | **1,826KB** |

**사진을 페이지 밖 파일로 빼면 첫 화면이 316KB** 로 줄어듭니다 (코드 212 + 표지 54 +
카드 50). 디자인이 몇 개든 첫 화면은 그대로고, **고른 디자인의 사진만 그때 받습니다.**
잃는 것은 `index.html` 을 더블클릭해 여는 것뿐인데, `preview.js` 가 이미 대신합니다.

> 계획: **삥은 지금 방식대로 넣고, 날개형을 넣는 시점에 사진 외부화를 같이 한다.**

### 결제

**아직 없습니다.** 지금은 **대표가 카톡에서 결제 링크를 손수 보냅니다** — 화면에도
「확인 후 결제하실 수 있는 링크를 보내드립니다」라고 적혀 있습니다.

방향은 정해져 있습니다 — 시뮬레이터에 결제를 직접 붙이지 않고
**카페24 자사몰 「바로구매 URL」** 로 넘깁니다 (대표에게 카페24 + 토스페이먼츠가 이미 있음).

**★ 제약** — 카페24 바로구매 URL은 **「추가 입력 옵션」을 붙이면 못 씁니다.**
그래서 색 정보를 그 칸으로 받으면 안 되고, **배송메시지에 짧은 주문코드**로 받습니다.
착수 전에 확인할 것들이 `README.md` 「결제 — 다음에 할 일」에 정리돼 있습니다.

**카톡 경로와 카페24 경로를 어떻게 이을지**는 아직 안 정했습니다. 손님이 카톡으로
주문을 보내면 대표가 값을 확정하고 바로구매 링크를 보내는 흐름이 자연스럽습니다.

### ★ 대표가 해야 할 일 (코드로 못 하는 것)

- **「카카오톡 채널 관리자」 앱을 깔고 알림이 오는지 확인.** 카카오톡과 **다른 앱**입니다.
  **이걸 안 깔면 주문이 와도 모릅니다.** 관리자센터 웹은 `business.kakao.com` 입니다.
- **채널 인사말 바꾸기.** 지금은 「안녕하세요. 무엇을 도와드릴까요?」입니다.
  주문 접수 창구로 쓰므로 「주문 내용을 붙여넣어 보내주시면 확인 후 결제 링크를
  보내드립니다」 같은 말이 낫습니다. **관리자센터에서만 고칠 수 있습니다.**
- **폰에서 끝까지 한 번 돌려보기.** 카톡 앱으로 넘어가 붙여넣기가 되는지는
  **폰에서만 알 수 있습니다.** 컴퓨터 브라우저 확인은 그 앞까지입니다.

### 자잘한 것

- ③ 머리글 아래 문구는 **작업자가 써넣은 것**입니다 (②④ 는 대표가 줌).
  대표에게 확인받거나 다시 쓰면 좋습니다.

---

## 12. 알아두면 좋은 사실들

- **화면에서 구별되지 않는 색쌍이 셋** 있습니다 (ΔE 2 미만). `907`↔`2008`(0.00),
  `931`↔`979`(1.36), `932`↔`980`(1.54). **원단이 실제로 그런 것이라 색상값으로는
  풀 수 없습니다.** 칩 밑의 번수·번호가 유일한 구분 단서입니다.
  결제를 붙이면 "화면에선 같아 보였다"가 분쟁이 될 수 있는 자리입니다.
- **컬러차트에 번호가 잘못 찍힌 칩이 둘** 있었습니다 — 다크 그린 `2027`→`2029`,
  코코아 그레이 `2023`→`2031`. `NO_FIX` 에서 바로잡습니다.
  **차트 자체는 아직 틀린 번호로 인쇄돼 있습니다.**
- **색상값은 검증됐습니다.** 102색 전부를 원본에서 전해상도로 다시 뽑아 대조한 결과
  최대 ΔE 0.9. **다시 뽑을 이유가 없습니다.**
- `양면.jpg`·`양면1.jpg` 를 오래 못 썼던 이유는 **흰 바탕이 없어서**였습니다.
  바탕이 생기니 `사진 ÷ 바탕` 으로 그늘이 지워져 부위가 깨끗하게 갈렸습니다.
  **「부위가 안 갈린다」와 「흰 바탕이 없다」를 구별하지 못하면 멀쩡한 사진을 버리게 됩니다.**
- 브라우저 확인은 `puppeteer-core` + 시스템 크롬으로 합니다
  (`C:\Program Files (x86)\Google\Chrome\Application\chrome.exe`).
  `npm i` 할 때는 **`sharp puppeteer-core subset-font` 를 한꺼번에** 설치하십시오 —
  `package.json` 이 없어서 `--no-save` 로 하나만 깔면 나머지가 지워집니다.
- **★ 페이지 안에서 도는 JS 에는 정규식의 역슬래시를 그대로 쓰면 안 됩니다.**
  그 코드는 `build.js` 의 **템플릿 리터럴 안**에 있어서 `\(` 의 역슬래시가 먹힙니다.
  `/\(([^)]+)\)/` 로 적었더니 페이지에는 `/(([^)]+))/` 로 나가 두께가
  「간절기용 (8온스」로 찍혔습니다. **겹쳐 쓰거나(`\\(`), 아예 역슬래시를 안 쓰는
  방식으로** 짜십시오 (`ozShort` 는 `indexOf` 로 다시 짰습니다).
  같은 이유로 `${` 도 그대로 못 씁니다. **주석까지 페이지에 그대로 실립니다** —
  그래서 왜 그렇게 했는지를 코드 옆에 적어두면 다음 사람이 화면에서도 볼 수 있습니다.
- 헤드리스로 단추를 눌러 확인할 때, **다른 도메인으로 넘어가는 것을 가로채 막으면
  그 순간 문서가 날아갑니다.** 클릭 뒤 화면을 읽으려면 **가려는 주소만 기록**하고
  DOM 은 클릭 전에 읽어두십시오. 실제 폰에서는 카톡 앱으로 넘어갈 뿐 페이지는 남습니다.

---

## 13. 전체를 다시 만드는 순서

```bash
npm i sharp puppeteer-core subset-font --no-save   # 한꺼번에

node src/tools/extract-masks.js     # 무지 마스크 + 흰 바탕
node src/tools/alpha-masks.js       # 알파 채널로 (건너뛰면 색이 안 먹음)
node src/tools/split-piping.js      # 양면 바탕 + 마스크 4장
node src/tools/design-cards.js      # 디자인 카드 2장
node src/tools/brand-hero.js        # ① 표지 사진
node src/tools/brand-logo.js        # 워드마크
node src/tools/extract-colors.js    # 컬러 102색

node src/build.js                   # index.html
node src/tools/subset-fonts.js      # 폰트 자르기
node src/build.js                   # 폰트 박기
```

**평소에는 마지막 셋만** 돌리면 됩니다.
**문구를 손댔으면 반드시 셋을 다 돌리십시오.**
